
package Kelas;
import java.util.*;
public class metodd {
    private static int fibo(int angka){
    if (angka<=1){
        return angka;
        }
        return fibo(angka-1)+(angka-2);
    }
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        
        System.out.println("masukkan jummlah yang ingin ditampilkan: ");
        int count = scanner.nextInt();
        for
    }
}
